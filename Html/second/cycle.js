// //напишите программу которая запрашивает у пользователя число и выводит на экран таблицу умножения этого числа от 1 до 10
// let userInput = prompt("Введите любое число: ");
// let number = +userInput;
// if (!isNaN(number)) {
//   for (let i = 1; i < number; i++) {
//     console.log(`${number} * ${i} = ${number * i}`);
//   }
// }

// //напишите программу которая запрашивает у пользователя число и выводит сумму всех чисел от 1 до этого числа
// // Запрашиваем у пользователя число
// const userInput = prompt("Введите число:");

// // Преобразуем введенное значение в целое число
// const number = parseInt(userInput, 10);

// // Проверяем, является ли введенное значение числом
// if (!isNaN(number) && number > 0) {
//   let sum = 0;

//   // Считаем сумму чисел от 1 до введенного числа
//   for (let i = 1; i <= number; i++) {
//     sum += i;
//   }

//   // Выводим результат
//   console.log(`Сумма всех чисел от 1 до ${number} равна ${sum}.`);
// } else {
//   // Если пользователь ввел не положительное число
//   console.log("Пожалуйста, введите положительное число.");
// }
// //напишите программу которая запрашивает у пользователя строку  и выводит на экран все символы этой строки в обратном порядке

// // Запрашиваем у пользователя строку
// const userInput = prompt("Введите строку:");

// // Проверяем, что строка не пуста
// if (userInput) {
//   // Переворачиваем строку
//   const reversedString = userInput.split("").reverse().join("");

//   // Выводим результат
//   console.log(`Строка в обратном порядке: ${reversedString}`);
// } else {
//   // Если пользователь не ввел строку
//   console.log("Вы не ввели строку.");
// }

// //напишите программу которая запрашивает у пользователя число и находит все простые числа меньше или равные этому числу
// //Функция для проверки, является ли число простым
// function isPrime(num) {
//   if (num < 2) {
//     return false;
//   }
//   for (let i = 2; i <= Math.sqrt(num); i++) {
//     if (num % i === 0) {
//       return false;
//     }
//   }
//   return true;
// }

// // Запрашиваем у пользователя число
// const userInput = parseInt(prompt("Введите число:"), 10);

// // Проверяем, что пользователь ввел корректное число
// if (isNaN(userInput)) {
//   console.log("Пожалуйста, введите корректное число.");
// } else {
//   const primes = [];

//   // Находим все простые числа до введенного числа
//   for (let i = 2; i <= userInput; i++) {
//     if (isPrime(i)) {
//       primes.push(i);
//     }
//   }

//   // Выводим результат
//   console.log(`Простые числа до ${userInput}: ${primes.join(", ")}`);
// }
